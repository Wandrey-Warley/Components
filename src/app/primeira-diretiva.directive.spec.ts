import { PrimeiraDiretivaDirective } from './primeira-diretiva.directive';

describe('PrimeiraDiretivaDirective', () => {
  it('should create an instance', () => {
    const directive = new PrimeiraDiretivaDirective();
    expect(directive).toBeTruthy();
  });
});
